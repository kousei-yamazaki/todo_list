# ToDo管理アプリケーション 依存パッケージ
# Tkinterは Python 標準ライブラリに含まれるため pip install 不要
# 以下は追加で必要なパッケージ（現時点では標準ライブラリのみで実装可能）

# 標準ライブラリのみ使用のため、追加インストール不要
# - tkinter (GUI)
# - json (データ保存)
# - uuid (ID生成)
# - os, pathlib (ファイル操作)
